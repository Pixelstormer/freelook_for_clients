# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 1.0.0
maven_group = com.pixelstorm
archives_base_name = freelook_for_clients
loader = quilt

# Dependencies are managed at gradle/libs.versions.toml
